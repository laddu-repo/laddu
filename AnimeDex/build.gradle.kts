version = 2

android {
    buildFeatures {
        buildConfig = true
    }
}

cloudstream {
    language = "en"
    description = "AnimeDex - Multi-source anime with sub & dub"
    authors = listOf("raghav")
    status = 1
    tvTypes = listOf("Anime", "AnimeMovie", "OVA")
    iconUrl = "https://raw.githubusercontent.com/laddu-repo/laddu/master/AnimeDex/logo.png"
}
